---
phase: 01-ci-cd-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ci-cd-gotchas.md
  - docs/deployment.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "docs/ci-cd-gotchas.md describes Gzip compression (not Brotli) and explains decompressionFallback approach"
    - "docs/ci-cd-gotchas.md lists UNITY_LICENSE, UNITY_EMAIL, UNITY_PASSWORD as GitHub Actions secrets"
    - "docs/ci-cd-gotchas.md references GitHub Actions (not GitLab CI) throughout"
    - "docs/deployment.md describes automatic deploy on push to main via GitHub Actions"
    - "docs/deployment.md references Gzip (not Brotli) and removes the Future GitLab CI section"
  artifacts:
    - path: "docs/ci-cd-gotchas.md"
      provides: "Accurate CI/CD troubleshooting reference for GitHub Actions + GameCI + Gzip + Cloudflare"
      contains: "Gzip"
    - path: "docs/deployment.md"
      provides: "Accurate deployment guide reflecting GitHub Actions auto-deploy on push to main"
      contains: "GitHub Actions"
  key_links:
    - from: "docs/ci-cd-gotchas.md"
      to: ".github/workflows/build.yml"
      via: "Secrets section lists correct GitHub Actions secret names"
      pattern: "UNITY_LICENSE.*UNITY_EMAIL.*UNITY_PASSWORD"
---

<objective>
Update two documentation files to accurately reflect the actual technology stack: Gzip compression (not Brotli), GitHub Actions with GameCI (not GitLab CI with manual license activation), and automatic push-to-main deploys (not tag-based manual deploys).

Purpose: The existing docs contain factually wrong information (Brotli, GitLab CI) that will mislead anyone debugging the pipeline. Accurate docs are a Phase 1 requirement (DOC-01, DOC-02).
Output: docs/ci-cd-gotchas.md and docs/deployment.md, both updated to reflect the actual system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ci-cd-pipeline/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/ci-cd-gotchas.md</name>
  <files>docs/ci-cd-gotchas.md</files>
  <read_first>
    - docs/ci-cd-gotchas.md (read full current content before making any changes — understand what's there)
    - .planning/REQUIREMENTS.md (DOC-01 exact acceptance criteria)
    - .planning/phases/01-ci-cd-pipeline/01-CONTEXT.md (D-12 exact requirements for this file)
    - .planning/phases/01-ci-cd-pipeline/01-RESEARCH.md (Pitfall 1 through 7 — use these as the source of truth for gotchas content)
  </read_first>
  <action>
Rewrite `docs/ci-cd-gotchas.md` to be accurate for the actual stack. Apply all of these changes:

**Changes required per D-12:**

1. **Replace the "Brotli, Not Gzip" section title and content** — The current section says "Brotli, Not Gzip" and describes `.br` files and `Content-Encoding: br`. This is factually wrong. Replace with:

```markdown
## Gzip, Not Brotli

Unity WebGL builds use Gzip compression (`.gz` files). `WebGLBuildScript.cs` sets `compressionFormat = WebGLCompressionFormat.Gzip` and `decompressionFallback = true`. This means the Unity JS loader decompresses files in-browser — Cloudflare does NOT need to serve `Content-Encoding: gz` headers.

The `_headers` file at the repo root sets `Cache-Control` headers only. Do not add `Content-Encoding` — Cloudflare Pages ignores it via `_headers` anyway, and `decompressionFallback` already handles decompression client-side.

Build output uses hashed filenames (`nameFilesAsHashes = true`): files are named like `Build/4a3b2c1d.wasm.gz`, not `Build/golf-game.wasm.gz`. Use wildcard patterns in `_headers`: `/Build/*` not `/Build/golf-game.*`.
```

2. **Replace the "GameCI Requires All Three Secrets" section** — The current section refers to GitLab CI variables. Replace with:

```markdown
## GitHub Actions Secrets

Set these as repository secrets in GitHub repo Settings > Secrets and variables > Actions:

- `UNITY_LICENSE` — Full contents of `Unity_lic.ulf` (Personal license). On Mac: `~/Library/Application Support/Unity/Unity_lic.ulf`. Copy the entire XML file content into the secret (no base64 encoding needed — GitHub secrets support multi-line values).
- `UNITY_EMAIL` — Unity account email address
- `UNITY_PASSWORD` — Unity account password
- `CLOUDFLARE_API_TOKEN` — API token with "Cloudflare Pages: Edit" permission (Cloudflare dashboard > My Profile > API Tokens)
- `CLOUDFLARE_ACCOUNT_ID` — Cloudflare account ID (Cloudflare dashboard right sidebar)

GameCI uses online activation (not manual `.ulf` file activation). All three Unity secrets are required — GameCI handles the activation process automatically.

**Personal license expiry:** Unity Personal licenses activate for ~30 days. When builds fail with "no valid Unity license," generate a new `.ulf` in Unity Hub and update the `UNITY_LICENSE` secret.
```

3. **Replace the "Disk Space" section** — Current section describes GitLab `before_script` approach. Replace with:

```markdown
## Disk Space (Unity 6 on GitHub Actions)

Unity 6 Docker images are 15+ GB. GitHub Actions ubuntu-latest provides ~14 GB free. The `build-and-deploy` job uses `jlumbroso/free-disk-space@v1.3.1` as its first step (before checkout) to free 20-30 GB:

```yaml
- uses: jlumbroso/free-disk-space@v1.3.1
  with:
    tool-cache: false
    android: true
    dotnet: true
    haskell: true
    large-packages: true
    docker-images: true
    swap-storage: true
```

This must be the first step — before checkout — to avoid "no space left on device" errors when Docker pulls the Unity image.
```

4. **Update the "GameCI Output Path" section** — Keep the content (build path is still correct) but add a note that this is for GitHub Actions:

```markdown
## GameCI Output Path

With `buildName: golf-game`, GameCI outputs to `build/WebGL/golf-game/`, not `build/WebGL/WebGL/`:
```yaml
# In build.yml deploy step
run: npx wrangler@^4 pages deploy build/WebGL/golf-game/ --project-name=golf-game
```
```

5. **Remove or update the "Cloudflare Pages Preview URLs" section** — The current content refers to `golf-game.pages.dev` (wrong). Update to:

```markdown
## Cloudflare Pages URL

Production URL: `https://golf-game-amm.pages.dev`

The `build-and-deploy` job deploys automatically on every push to `main`. No manual tagging or `workflow_dispatch` needed.
```

6. **Remove the "Branch Naming (Cloudflare Pages)" section** — No longer relevant; deployment is push-to-main only (no branch selector in wrangler command, Cloudflare Pages maps it to production automatically).

7. **Remove the "SemVer Pre-release Tags" section** — Deploys are no longer tag-based.

8. **Keep the "LFS Dirty Build Error" section** — Still accurate. Update the code example to use GitHub Actions syntax:

```markdown
## LFS Dirty Build Error

`.lfs-assets-id` file (created to generate the LFS cache key) causes "dirty build" errors. It is deleted after LFS pull:
```yaml
- name: Pull LFS files
  run: git lfs pull && rm -f .lfs-assets-id
```
```

9. **Keep the "Same Build Type for Staging and Production" section** — Still valid.

10. **Add a new "Unity Version" section** at the top:

```markdown
## Unity Version

The project uses Unity `6000.3.10f1`. The `build.yml` workflow hardcodes `unityVersion: 6000.3.10f1`. Using the wrong version (`6000.0.23f1` appeared in old workflows) causes GameCI to pull the wrong Docker image and may cause license activation failure.
```

**Final check:** After rewriting, verify the file contains zero instances of:
- "Brotli" or ".br" (in the context of Unity WebGL compression)
- "GitLab" or "gitlab-ci"
- `UNITY_LICENSE.*base64` (no base64 encoding instruction)
- `golf-game.pages.dev` without the `-amm` suffix
  </action>
  <verify>
    <automated>grep -c "Gzip" /Users/ashleyrevlett1/Documents/apps/golf-game/docs/ci-cd-gotchas.md && ! grep -q "Brotli, Not Gzip" /Users/ashleyrevlett1/Documents/apps/golf-game/docs/ci-cd-gotchas.md && ! grep -q "gitlab-ci" /Users/ashleyrevlett1/Documents/apps/golf-game/docs/ci-cd-gotchas.md && grep -q "UNITY_LICENSE" /Users/ashleyrevlett1/Documents/apps/golf-game/docs/ci-cd-gotchas.md && grep -q "CLOUDFLARE_API_TOKEN" /Users/ashleyrevlett1/Documents/apps/golf-game/docs/ci-cd-gotchas.md && echo "PASS"</automated>
  </verify>
  <done>
    - `docs/ci-cd-gotchas.md` contains "Gzip" (not "Brotli, Not Gzip" as section title)
    - File does NOT contain "Brotli" as compression description for Unity WebGL
    - File does NOT contain "gitlab-ci" or "GitLab CI variables"
    - File contains all 5 secret names: UNITY_LICENSE, UNITY_EMAIL, UNITY_PASSWORD, CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID
    - File explains decompressionFallback approach (no Content-Encoding needed)
    - File references `golf-game-amm.pages.dev` (not `golf-game.pages.dev`)
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs/deployment.md</name>
  <files>docs/deployment.md</files>
  <read_first>
    - docs/deployment.md (read full current content before making any changes)
    - .planning/REQUIREMENTS.md (DOC-02 exact acceptance criteria)
    - .planning/phases/01-ci-cd-pipeline/01-CONTEXT.md (D-13 exact requirements for this file)
    - .github/workflows/build.yml (read after Task 1 in plan 01-01 creates it — use actual job/step names in docs)
  </read_first>
  <action>
Rewrite `docs/deployment.md` to describe the actual current deployment approach. Apply all of these changes per D-13:

**New content for docs/deployment.md:**

```markdown
# Deployment

## Architecture

```
GitHub push to main → GitHub Actions (build.yml) → game-ci/unity-builder → npx wrangler@^4 → Cloudflare Pages → Users
```

## Cloudflare Pages

- Project name: `golf-game`
- Production URL: `https://golf-game-amm.pages.dev`
- Compression: Gzip (Unity WebGL with `decompressionFallback = true`)

## Automated Deploy (GitHub Actions)

Every push to `main` triggers the `build-and-deploy` job in `.github/workflows/build.yml`:

1. Frees disk space (Unity 6 images are 15+ GB)
2. Checks out repo with LFS files
3. Caches Unity Library folder (avoids ~10 min re-import on cache hit)
4. Builds WebGL via `game-ci/unity-builder@v4` using `WebGLBuildScript.Build`
5. Copies `_headers` into `build/WebGL/golf-game/`
6. Deploys to Cloudflare Pages via `npx wrangler@^4 pages deploy`

No manual tagging or intervention required — push to main and the deploy happens automatically.

## Required GitHub Secrets

Configure these in GitHub repo Settings > Secrets and variables > Actions before the pipeline will work:

| Secret | Purpose | Where to Get |
|--------|---------|--------------|
| `UNITY_LICENSE` | GameCI license activation | Full contents of `~/Library/Application Support/Unity/Unity_lic.ulf` |
| `UNITY_EMAIL` | GameCI license activation | Unity account email |
| `UNITY_PASSWORD` | GameCI license activation | Unity account password |
| `CLOUDFLARE_API_TOKEN` | wrangler deploy auth | Cloudflare dashboard > API Tokens (Pages:Edit permission) |
| `CLOUDFLARE_ACCOUNT_ID` | wrangler deploy target | Cloudflare dashboard right sidebar |

## Manual Deploy (Local)

For one-off deploys without pushing to main:

```bash
# 1. Build WebGL
/Applications/Unity/Hub/Editor/6000.3.10f1/Unity.app/Contents/MacOS/Unity \
  -batchmode -nographics -quit \
  -projectPath ~/Documents/apps/golf-game \
  -executeMethod WebGLBuildScript.Build \
  -logFile /tmp/unity-build.log

# 2. Copy _headers into build output
cp _headers build/WebGL/golf-game/_headers

# 3. Deploy to Cloudflare Pages
export CLOUDFLARE_API_TOKEN=<your-token>
export CLOUDFLARE_ACCOUNT_ID=<your-account-id>

npx wrangler@^4 pages deploy build/WebGL/golf-game/ \
  --project-name=golf-game \
  --commit-dirty=true
```

## Compression

Unity WebGL builds use Gzip (`.gz` files). `WebGLBuildScript.cs` sets:
- `compressionFormat = WebGLCompressionFormat.Gzip`
- `decompressionFallback = true` — Unity JS loader decompresses files client-side
- `nameFilesAsHashes = true` — build files have hashed names like `Build/4a3b2c1d.wasm.gz`

Cloudflare Pages does NOT need to set `Content-Encoding` headers. The `_headers` file at the repo root sets `Cache-Control` only.
```

**Ensure these specific removals happen:**
- Remove the "Future: GitLab CI Deploy" section (the TODO about self-hosted GitLab runner) — per D-13
- Remove any reference to "Brotli-compressed" — replace with "Gzip"
- Remove tag-based deploy instructions from the main flow (keep manual deploy section but update it to use wrangler directly)
- Remove the "Versioning" section about `git tag && git push --tags` as a deploy trigger (tagging is no longer how deploys work)
  </action>
  <verify>
    <automated>grep -q "GitHub Actions" /Users/ashleyrevlett1/Documents/apps/golf-game/docs/deployment.md && grep -q "golf-game-amm.pages.dev" /Users/ashleyrevlett1/Documents/apps/golf-game/docs/deployment.md && ! grep -q "Brotli-compressed" /Users/ashleyrevlett1/Documents/apps/golf-game/docs/deployment.md && ! grep -q "Future: GitLab" /Users/ashleyrevlett1/Documents/apps/golf-game/docs/deployment.md && grep -q "wrangler@\^4" /Users/ashleyrevlett1/Documents/apps/golf-game/docs/deployment.md && echo "PASS"</automated>
  </verify>
  <done>
    - `docs/deployment.md` contains "GitHub Actions" as the CI platform
    - File contains `golf-game-amm.pages.dev` as the production URL
    - File does NOT contain "Brotli-compressed"
    - File does NOT contain "Future: GitLab CI Deploy" or "TODO: Set up self-hosted GitLab runner"
    - File contains `wrangler@^4` in deploy commands
    - File describes automatic deploy on push to main
    - File lists all 5 required GitHub secrets in a table
    - File explains Gzip + decompressionFallback approach
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# DOC-01: ci-cd-gotchas.md checks
grep -q "Gzip" docs/ci-cd-gotchas.md && echo "DOC-01: Gzip present"
! grep -q "Content-Encoding: br" docs/ci-cd-gotchas.md && echo "DOC-01: no Brotli Content-Encoding"
grep -q "UNITY_LICENSE" docs/ci-cd-gotchas.md && echo "DOC-01: UNITY_LICENSE documented"
grep -q "CLOUDFLARE_API_TOKEN" docs/ci-cd-gotchas.md && echo "DOC-01: CLOUDFLARE_API_TOKEN documented"
! grep -q "gitlab-ci" docs/ci-cd-gotchas.md && echo "DOC-01: no GitLab CI references"

# DOC-02: deployment.md checks
grep -q "GitHub Actions" docs/deployment.md && echo "DOC-02: GitHub Actions present"
grep -q "golf-game-amm.pages.dev" docs/deployment.md && echo "DOC-02: correct URL"
! grep -q "Future: GitLab" docs/deployment.md && echo "DOC-02: GitLab TODO removed"
! grep -q "Brotli" docs/deployment.md && echo "DOC-02: no Brotli references"
```
</verification>

<success_criteria>
1. `docs/ci-cd-gotchas.md` describes Gzip (not Brotli), explains decompressionFallback, lists all 5 GitHub secrets, references GitHub Actions (not GitLab), and includes the correct production URL
2. `docs/deployment.md` describes GitHub Actions push-to-main auto-deploy, references Gzip, has no "Future: GitLab CI" TODO, lists required secrets in a table, and uses the correct URL `golf-game-amm.pages.dev`
</success_criteria>

<output>
After completion, create `.planning/phases/01-ci-cd-pipeline/01-02-SUMMARY.md` with:
- What was changed in each doc file
- Old content replaced (Brotli → Gzip, GitLab → GitHub Actions)
- Any sections added, removed, or kept unchanged
</output>
